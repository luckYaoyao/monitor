const DEFAULT_CONFIG = {
    // 项目id
    pid: '',
    // 设备id
    uid: '',
    // 用户sessionId
    sid: '',
    record: {
        // 是否监控页面错误
        jsError: true,
        // 是否监控页面性能
        performance: true,
        // 是否监控用户在线时长数据
        timeOnPage: true,
    }
}